These files are internal to XLA and should not be exposed externally.
Integrators should use the PJRT API. See https://openxla.org/xla/pjrt_integration

Even tools should NOT use these files directly and instead use the PJRT API to
ensure consistently with usage.
